import { Text, View } from "react-native";

export default function NotesList({ plusCode }: { plusCode: string }) {
  return (
    <View>
      <Text>Notes list coming soon for {plusCode}</Text>
    </View>
  );
}
