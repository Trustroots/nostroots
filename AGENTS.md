# Agent Notes

Follow the repo guidance in `CLAUDE.md`.

For `vibe/web` browser checks, use the existing local server at `http://localhost:30397/` when available instead of asking to start another static server.
